# String Reversal

This program will take a line of user input and reverse that line.

It will keep running until the user types in 'quit'.

```
$ g++ -o solution.exe src/solution.cpp
$ ./solution.exe
```

