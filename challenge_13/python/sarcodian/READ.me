Uses a loop with // 10 to get how many digits in the number.

Then compares the first and last using % 10 (for the last)
and // 10 ** length-1 (for the first). Uses same concept to 
delete the two digits for the next pass. 

Loops over the number length // 2 times.
